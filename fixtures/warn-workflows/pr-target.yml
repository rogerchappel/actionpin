name: review-labeler
on:
  pull_request_target:
permissions:
  contents: read
  pull-requests: write
jobs:
  label:
    runs-on: ubuntu-latest
    steps:
      # actionpin allow: actions.unpinned
      - uses: actions/labeler@v5
      - run: echo "labels only"
